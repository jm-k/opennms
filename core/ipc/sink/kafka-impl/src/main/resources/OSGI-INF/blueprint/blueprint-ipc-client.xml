<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
    xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0"
    xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0 
		http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0
		http://aries.apache.org/schemas/blueprint-ext/blueprint-ext-1.1.xsd
">

    <cm:property-placeholder id="kafkaProperties" persistent-id="org.opennms.core.ipc.sink.kafka" update-strategy="reload">
      <cm:default-properties>
        <cm:property name="kafkaAddress" value="127.0.0.1:9092" />
        <cm:property name="zookeeperHost" value="127.0.0.1" />
        <cm:property name="zookeeperPort" value="2181" />
        <cm:property name="groupId" value="kafkaMessageConsumer" />
      </cm:default-properties>
    </cm:property-placeholder>

    <!-- Provided by minion-core -->
    <reference id="minionIdentity" interface="org.opennms.minion.core.api.MinionIdentity" />

    <bean id="partitionKeyGenerator" class="org.opennms.core.camel.RandomPartitionKeyGenerator"/>

	<bean id="kafkaRemoteMessageDispatcherFactory" class="org.opennms.core.ipc.sink.kafka.KafkaRemoteMessageDispatcherFactory"
	  init-method="afterPropertiesSet" destroy-method="unregister">
	  <property name="kafkaAddress" value="${kafkaAddress}" />
	  <property name="zookeeperHost" value="${zookeeperHost}" />
	  <property name="zookeeperPort" value="${zookeeperPort}" />
	  <property name="groupId" value="${groupId}" />
	</bean>

    <service ref="kafkaRemoteMessageDispatcherFactory" interface="org.opennms.core.ipc.sink.api.MessageDispatcherFactory"/>

    <!-- this exists only to make the CamelBlueprintTest happy -->
    <camelContext id="sinkClient" xmlns="http://camel.apache.org/schema/blueprint">
    </camelContext>

</blueprint>
